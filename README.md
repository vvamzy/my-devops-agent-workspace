directory_structure = { 'scripts': [], 'docs': [], '.github': { 'workflows': ['ci.yml'] } }
readme_content = '# my-devops-agent-workspace

This repository serves as a workspace for an autonomous DevOps Agent. The agent runs once per day, performing tasks as outlined in the `.agent_manifest.json` file.'